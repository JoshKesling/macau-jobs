.row
  .col-sm-10.col-sm-offset-1
    .panel.panel-primary
      .panel-heading.text-center
        - if @page_title
          %h2
            = @page_title
      .panel-body
        - if @partial_to_render
          = render @partial_to_render